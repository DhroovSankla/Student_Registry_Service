
spring.datasource.url=jdbc:mysql://localhost:3306/student_registry_test_db
spring.datasource.username=root
spring.datasource.password=root

# Keep Flyway active so it builds the clean test schema tables automatically
spring.flyway.enabled=true

# Prevent Hibernate from altering things dynamically
spring.jpa.hibernate.ddl-auto=validate